name: Publish @nsmithau/xrpl-connect

on:
  push:
    branches: [develop]
  workflow_dispatch:

concurrency:
  group: publish-nsmithau-xrpl-connect
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10.18.3

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          registry-url: https://npm.pkg.github.com
          scope: "@nsmithau"
          cache: pnpm

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Build + finalize package
        run: |
          pnpm --filter xrpl-connect publish:build
          node packages/xrpl-connect/scripts/nsmithau-finalize-publish.mjs

      - name: Publish to GitHub Packages
        working-directory: packages/xrpl-connect/dist-publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm publish --access public --tag develop
